# C2 Release Record: v0.1.0

Date: 2026-09-08

## Result

The first public GitHub Release is complete and its downloadable installation archive has been validated in a clean ComfyUI environment.

- Release: <https://github.com/zihaomu/comfyui-minimax-h3-remote/releases/tag/v0.1.0>
- Tag: `v0.1.0`
- Target commit: `0fc7ce065fe51c6847480a094370156422616cda`
- Published: `2026-09-08T03:31:33Z`
- Prerelease: no
- Draft: no

## Release Assets

| Asset | Size | SHA-256 |
|---|---:|---|
| `comfyui-minimax-h3-remote-v0.1.0.zip` | 17,625 B | `825652a52e6613ba5d3ffe9cac7895ac92e53dab9434af0f7b53007bffe3963d` |
| `comfyui-minimax-h3-remote-v0.1.0.zip.sha256` | 103 B | GitHub asset digest `31f605d404282adcb181de9fc9ca2ff37de88f7f469362ab0293196df26af767` |

The installation ZIP contains only the runtime node, Apache-2.0 license, README, changelog, Registry metadata, and four workflow templates. It excludes `.git`, `.github`, tests, scripts, design documentation, caches, and credentials.

## Post-Release Verification

1. Downloaded both assets from GitHub Release.
2. `sha256sum -c` passed.
3. Extracted the ZIP into a clean `ComfyUI/custom_nodes` directory.
4. Verified version `0.1.0` and four ComfyUI v1 workflow files.
5. Started ComfyUI v0.34.0 with `--cpu` using the extracted release package.
6. `GET /object_info/MiniMaxH3Remote` returned the native `VIDEO`, `IMAGE`, `AUDIO`, `FLOAT`, and `STRING` outputs with `output_node=true`.
7. `GET /workflow_templates` reported all four templates.

## Registry Status

ComfyUI Registry publication remains pending. The GitHub repository currently has no `REGISTRY_ACCESS_TOKEN` secret, so no Registry publish action was attempted. This is intentional: a GitHub Release is complete, while Registry publication requires the repository owner to create a Registry publisher identity and token.
